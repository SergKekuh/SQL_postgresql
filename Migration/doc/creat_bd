-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.client_activity
(
    client_activity_id serial NOT NULL,
    unit_id integer NOT NULL,
    year_id integer NOT NULL,
    group_id integer NOT NULL,
    expense_invoices numeric(15, 2),
    sales numeric(15, 2),
    note text COLLATE pg_catalog."default",
    CONSTRAINT client_activity_pkey PRIMARY KEY (client_activity_id),
    CONSTRAINT client_activity_unit_id_year_id_key UNIQUE (unit_id, year_id)
);

CREATE TABLE IF NOT EXISTS public.groups
(
    group_id serial NOT NULL,
    group_name character varying(50) COLLATE pg_catalog."default" NOT NULL,
    note text COLLATE pg_catalog."default",
    CONSTRAINT groups_pkey PRIMARY KEY (group_id),
    CONSTRAINT groups_group_name_key UNIQUE (group_name)
);

CREATE TABLE IF NOT EXISTS public.client_units
(
    unit_id serial NOT NULL,
    okpo_code character varying(12) COLLATE pg_catalog."default" NOT NULL,
    okpo_s1c8 character varying(12) COLLATE pg_catalog."default",
    ipn character varying(12) COLLATE pg_catalog."default",
    unit_name character varying(255) COLLATE pg_catalog."default",
    full_unit_name text COLLATE pg_catalog."default",
    legal_entity_type character varying(10) COLLATE pg_catalog."default",
    note text COLLATE pg_catalog."default",
    CONSTRAINT client_units_pkey PRIMARY KEY (unit_id)
);

CREATE TABLE IF NOT EXISTS public.years
(
    year_id serial NOT NULL,
    year integer NOT NULL,
    note text COLLATE pg_catalog."default",
    CONSTRAINT years_pkey PRIMARY KEY (year_id),
    CONSTRAINT years_year_key UNIQUE (year)
);

ALTER TABLE IF EXISTS public.client_activity
    ADD CONSTRAINT client_activity_group_id_fkey FOREIGN KEY (group_id)
    REFERENCES public.groups (group_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.client_activity
    ADD CONSTRAINT client_activity_unit_id_fkey FOREIGN KEY (unit_id)
    REFERENCES public.client_units (unit_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.client_activity
    ADD CONSTRAINT client_activity_year_id_fkey FOREIGN KEY (year_id)
    REFERENCES public.years (year_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;

END;